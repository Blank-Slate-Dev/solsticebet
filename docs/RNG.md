# Provably-fair RNG Specification

🚧 **Pending.** Lands with the `@solsticebet/rng` implementation.

Will contain:

- Full algorithm specification (HMAC-SHA256 derivation)
- Server seed lifecycle (generation, hash publication, rotation, reveal)
- Client seed semantics
- Nonce semantics
- Per-game outcome derivation (Dice, Mines, Crash, Plinko)
- Reference verification implementation (vanilla JS, runs offline)
- Certification submission package contents

See `ARCHITECTURE.md` § 7 for the architectural overview.
