{
  "name": "@solsticebet/admin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Internal admin panel",
  "scripts": {
    "build": "echo 'Next.js scaffold pending — see docs/ARCHITECTURE.md § 5.3' && exit 0",
    "dev": "echo 'Next.js scaffold pending — see docs/ARCHITECTURE.md § 5.3'",
    "lint": "echo 'pending'",
    "typecheck": "echo 'pending'",
    "test": "echo 'pending'",
    "clean": "rm -rf dist .next .turbo *.tsbuildinfo"
  }
}
